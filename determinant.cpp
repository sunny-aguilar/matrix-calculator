//
#include "determinant.hpp"
#include <iostream>
using std::cout;
using std::endl;

int determinant(int **matrixPtr, int size) {
    if (size == 2) {

    }
    else if (size == 3) {
        
    }

    return 0;
}